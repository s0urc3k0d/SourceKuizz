{
  "name": "sourcekuizz-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "eslint . --ext .ts,.tsx || true",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "eslint": "^8.57.0",
    "@types/node": "^20.11.0",
    "concurrently": "^8.2.2"
  }
}
